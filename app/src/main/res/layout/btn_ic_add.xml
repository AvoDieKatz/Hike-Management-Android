<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.button.MaterialButton
        style="@style/Widget.Material3.Button.IconButton"
        android:id="@+id/observation_add_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:icon="@drawable/ic_add"
        app:iconGravity="textStart"
        android:gravity="center"
        />

    <TextView
        android:id="@+id/add_btn_lbl"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Add an observation"
        tools:layout_editor_absoluteX="38dp"
        tools:layout_editor_absoluteY="9dp"
        app:layout_constraintTop_toBottomOf="@id/observation_add_btn"
        app:layout_constraintStart_toStartOf="@id/observation_add_btn"
        app:layout_constraintEnd_toEndOf="@id/observation_add_btn"
        />


</androidx.constraintlayout.widget.ConstraintLayout>